# 强制 Nixpacks 使用 JDK 21 构建（pom.xml 要求 Java 21）
[nixpacks]
nixPkgs = ["jdk21", "maven"]

# 构建阶段：补 mvnw 可执行权限后打包
[phases.build]
cmds = ["chmod +x mvnw", "./mvnw -B package -DskipTests"]

# 运行阶段用 JDK 21 启动 jar
[phases.start]
cmds = ["echo 'start phase'"]
