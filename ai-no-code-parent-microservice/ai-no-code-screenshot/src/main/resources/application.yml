spring:
  application:
    name: ai-no-code-screenshot
  profiles:
    active: local
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
server:
  port: 8127
  servlet:
    context-path: /api
# 对象存储
cos:
  client:
    accessKey: xxx
    secretKey: xxx
    region: xxx
    bucket: xxx
    host: xxx
# Dubbo
dubbo:
  registry:
    address: nacos://127.0.0.1:8848?username=nacos&password=nacos
    register-mode: instance
  protocol:
    name: tri
    port: 50052
  consumer:
    timeout: 60000
  provider:
    timeout: 60000
  application:
    qos-enable: true
    # 设置为-1，会自动随机找到一个可用端口，不然有可能启动服务一直会占用端口
    qos-port: -1