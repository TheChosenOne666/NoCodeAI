VITE_DEPLOY_DOMAIN=https://xiaolou-nocode.pages.dev

VITE_API_BASE_URL=https://nocodeai-production.up.railway.app/api

# COS 静态网站域名 + Cloudflare Worker 反向代理（剥离强制下载头）。
# 上游真实存储: https://xiaolou-bi-1382226492.cos-website.ap-shanghai.myqcloud.com
# 浏览器实际访问: VITE_COS_DEPLOY_HOST（本配置值），由 cos-view-proxy Worker 反向代理
# 上游 COS 并删除 content-disposition / x-cos-force-download 响应头，
# 避免浏览器把 HTML 资源当下载。无备案域名时的免备案展示方案。
# 需先在 COS 控制台开启「静态网站」（索引 index.html），并部署 cos-view-proxy Worker。
VITE_COS_DEPLOY_HOST=https://cos-view-proxy.1526655958.workers.dev